function greet(name) {

    console.log("welcome, " + name + ". " + "How's your day been?");
}
console.log(greet("Carl"));

for (let index = 0; index < array.length; index++) {
    const element = array[index];
    
}